
# DDS Helper configuration
helper:
  only-with-type: false

# DDS configuration
dds:
  # Domain ID
  domain: 0

  # Allowed topics
  allowlist:
    - name: "*"

  # Blocked topics
  blocklist:
    - name: "add_blocked_topics_list_here"

specs:
  threads: 12
  max-pending-samples: 20
  logging:
    stdout: false
    verbosity: info
